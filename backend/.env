PORT=5001
DATABASE_URL="postgresql://admin:password123@localhost:5432/cinecast?schema=public"

# App public URL — used in password-reset email links
APP_URL=http://localhost:5173

# SMTP email config — leave empty to use console-log fallback (dev mode)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM="CineCAST" <noreply@cinecast.sk>